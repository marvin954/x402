{
  "name": "mammba-x402-marketplace",
  "version": "1.0.0",
  "description": "Multi-tenant x402 API marketplace with per-transaction revenue split",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start":   "node src/index.js",
    "dev":     "NODE_ENV=development node --watch src/index.js",
    "test":    "node scripts/test.js",
    "migrate": "node scripts/migrate.js",
    "seed":    "node scripts/seed.js"
  },
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "cors":    "^2.8.5",
    "express": "^4.18.2",
    "pg":      "^8.11.3"
  }
}
